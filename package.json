{
  "name": "clamp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "lint": "eslint . --ext .vue,.js,.jsx,.cjs,.mjs,.ts,.tsx,.cts,.mjs --fix --ignore-path .gitignore",
    "format": "prettier --write .",
    "build:ssg": "nuxt generate",
    "deploy:twig": "npm run build:ssg && node scripts/deploy-to-twig.js"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "@nuxt/eslint-config": "^0.2.0",
    "cross-env": "^7.0.3",
    "eslint": "^8.57.0",
    "nuxt": "^3.12.4",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.5.1",
    "pinia": "^2.1.7"
  }
}